{
  "name": "elite-online",
  "version": "1.0.0",
  "description": "Sitio ELITE Online + API de captura de leads (Brevo) en un solo servicio Node.",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "build": "echo build",
    "start": "node app.js"
    "dev": "node --watch app.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0"
  }
}
